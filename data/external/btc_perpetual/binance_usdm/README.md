# BTC USD-M Perpetual External Data

This directory is reserved for local Binance USD-M perpetual public market-data bundles.

Do not commit downloaded market data. The `bundles/` directory is ignored by git by default.
Only the directory contract and placeholder files belong in source control.
